public class Book extends Item {

    public Book(int id, String type, String title, String author, String publisher, int publication_year) {
        super(id, type, title, author, publisher, publication_year);
    }
}
