# Week-3-Biblioteca


  1. Compilar (solo una vez):

  rm -rf out
  mkdir -p out
  javac -d out src/main/java/model/*.java src/main/java/dao/*.java src/main/java/view/*.java src/main/java/controller/*.java
  src/main/java/*.java

  2. Ejecutar sistema:

  java -cp out Main